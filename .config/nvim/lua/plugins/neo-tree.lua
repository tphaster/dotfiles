return {
  "nvim-neo-tree/neo-tree.nvim",
  opts = {
    filesystem = {
      follow_current_file = { enabled = false },
    },
    buffers = {
      follow_current_file = { enabled = false },
    },
  },
}
